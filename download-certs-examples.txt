# SMTPS example
openssl s_client -connect smtp.example.com:25 -name your.server.tld -starttls smtp -showcerts > smtp.example.com.pem
# HTTPS example
openssl s_client -connect https.example.com:443 -showcerts > https.example.com.pem
# Afterwards, delete extra text lines around the SSL certificate blocks.
